import React from "react";
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  ScrollView,
} from "react-native";

import { globalStyles } from "../styles/global";

export default function Estilos() {
  return (
    <ScrollView style={globalStyles.skContainer}>
      <Text>ESTILOS DE VIA</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({});