import React from "react";
import {
  StyleSheet,
  Text,
  View,
  Image,
  TouchableOpacity,
  ScrollView,
} from "react-native";

import { globalStyles } from "../styles/global";

export default function Ciencia() {
  return (
    <ScrollView style={globalStyles.skContainer}>
      <Text>CIÊNCIA E TECNOLOGIA</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({});